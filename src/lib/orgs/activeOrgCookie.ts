/**
 * Shared cookie name for the "active org/workspace" context.
 *
 * NOTE: Do not export arbitrary symbols from Next.js route modules (e.g. `src/app/.../route.ts`)
 * because Next's type-checker requires route modules to export only handler/config fields.
 */
export const ACTIVE_ORG_COOKIE = "ld_active_org";

