This might be an updated version of the deck because this is a replacement upload - or it might be
the same file uploaded again. Do not assume something changed.

Compare the PREVIOUS version with the NEW one and summarize what changed. Use both the extracted
text below and the rendered page images attached after it, where those are available. The text
carries wording, names, dates and figures written as prose; the images carry everything the text
extraction drops - charts, tables rendered as graphics, diagrams, logos and marks, photographs,
and the layout itself.

Things worth catching that the text alone will never show:
- a logo or wordmark added, removed or replaced, including a swap to a different company's mark
- a chart whose bars, lines or axis values moved
- a table whose numbers changed but whose headings did not
- a figure, screenshot or photograph replaced with a different one
- a slide that was redesigned while keeping the same words

PREVIOUS (PDF-extracted text):
{{PREVIOUS_TEXT}}

NEW (PDF-extracted text):
{{NEW_TEXT}}

CHANGED PAGES (page-level context, when available):
{{CHANGED_PAGES}}

Return ONLY the JSON output described in the system prompt.
