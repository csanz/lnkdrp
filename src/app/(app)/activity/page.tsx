/**
 * Page for `/activity` (authenticated app shell).
 *
 * Workspace activity feed: uploads, share changes, request submissions and download requests.
 */
import ActivityPageClient from "./pageClient";

export default function ActivityPage() {
  return <ActivityPageClient />;
}
