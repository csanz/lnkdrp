/**
 * Legacy alias:
 * `/replace/:token` re-exports the new `/doc/update/:code` page in `page.tsx`.
 * Keep this file as a no-op to avoid breaking imports/history.
 */
export default function ReplaceUploadPageClient() {
  return null;
}


