# Release notes, 2026-09-25

What ships when `next-release` merges into `main` (`fc72740`). The release is the commit the PR
merges; **reconciled on 2026-09-26, when that is `fbede1d`**: 83 commits from 2026-09-23 to
2026-09-26, 543 files. The first draft of these notes was written against `7f98f91`; everything the
fifteen commits since then added is in "Added after the first draft" at the end, and the corrections
they forced are in place above. Grouped by who notices. The deploy steps are in
`docs/deploy/checklist-2026-09-25.md`; the day-by-day account is `docs/CHANGELOG-2026-09-24.md`
and `docs/CHANGELOG-2026-09-25.md`. Yearly Pro, the em-dash sweep and the Mongo access diagnostics
are already on `main` (`bb19104` and its neighbours) and are not repeated here.

## Breaking and behaviour changes, read these first

1. **Free view emails no longer link to the reader page.** That page is Pro-only and answered
   "No reader by that id" from the button in the mail (review M24). On Free the primary button
   goes to the document's metrics page (`/doc/:id/metrics?shareId=...`) and the secondary
   "See who opened it" to `/pricing?from=view_email`. Pro emails are unchanged.
   `src/lib/notifications/viewNotifications.ts`, `tests/lib/viewNotifications.test.ts`.
2. **`/preferences` redirects to the dashboard.** The standalone copy of the account, workspace,
   usage and billing tabs is deleted (900 lines that had drifted; the infinite invite refetch of
   review H8 lived only there). `/preferences?tab=x` and `/preferences/x` go to
   `/dashboard?tab=x` (`spending` maps to `billing`, unknown tabs to `account`); the fragment in
   old view emails (`#email-preferences`) survives the redirect. `src/app/preferences/page.tsx`,
   `[tab]/page.tsx`, `tabs.ts`; `src/app/preferences/WorkspaceManager.tsx` and
   `preferencesShell.tsx` removed.
3. **MCP confirmation policy for destructive tools** (`delete_doc`, `archive_doc`,
   `delete_share_link`, `delete_project`, `delete_project_link`). A prompt that fails to reach a
   human (timeout, transport error, or an instant `cancel`) now refuses the call whatever
   `confirm` says, returns the preview, and remembers the failure for ten minutes; only the next
   call for the same action may carry `confirm: true`. A pre-set `confirm: true` on the first
   call is never honoured on a client that declared elicitation. A human's decline is final.
   Before this a cancel or timeout let a pre-set flag through (review M14). Preview headlines and
   facts are now sanitised like every other document-derived string (M15). `mcp/src/confirm.ts`,
   `docs/MCP.md` "Confirmation", `tests/lib/mcpConfirm.test.ts`.
4. **Inline upload limits over MCP.** `fileBase64` and `filePath` on `lnkdrp_share_pdf` and
   `lnkdrp_replace_pdf` accept at most **16 MB** before optimisation (was 50 MB, refused from the
   base64 length before decoding), and against a hosted API what is sent after optimisation must
   be under **3 MB** or the tool answers `too_large` naming the size and pointing at `sourceUrl`
   (which keeps the 50 MB document ceiling). The `/mcp` body limit is 24 MB with a JSON-RPC 413,
   and the bearer check runs before the body is buffered (review H4: an anonymous 66 MB POST used
   to be parsed first). Ghostscript and pdfjs run at most two at a time. `mcp/src/inlineLimits.ts`,
   `semaphore.ts`, `optimize.ts`, `main.ts`.
5. **MCP session caps.** 20 live sessions per credential (the stalest is closed to admit a new
   one) and 500 per process (`initialize` answers JSON-RPC 429). `mcp/src/sessionCaps.ts`,
   `LNKDRP_MCP_MAX_SESSIONS_PER_KEY`, `LNKDRP_MCP_MAX_SESSIONS`. Idempotency replays are now
   scoped per credential (200 entries each, 5,000 in total) so one busy key cannot evict
   another's (`mcp/src/idempotency.ts`).
6. **There is no personal workspace, only workspaces.** The one an account starts with is a
   workspace like any other. The Free cap is **two owned workspaces, whichever they are**
   (`FREE_WORKSPACES = 2` in `src/lib/billing/planLimits.ts`, replacing "one team workspace beside
   the personal one"; the `team_workspaces` limit key keeps its name); invites work in every
   workspace, gated by the collaborator limit alone; a share page signs with the workspace's
   name, falling back to the owner only while the name is still the placeholder
   (`src/lib/orgs/defaultName.ts`); the MCP connection name is `lnkdrp-<workspace>` in every case
   (`lnkdrp-personal` only while the workspace is still called Personal). Every screen and help
   article that said "personal" or "team workspace" now says "workspace". Commits `f0216f2`,
   `e867788`, `30031d4`.
7. **A recipient's upload secret is read-only once the upload completes** (review H1). A
   recipient who kept the secret from a request or replace link could mint a new Blob token and
   swap the bytes of a completed version in place, with no new version, no compare and no email.
   The Blob token route, `PATCH /api/uploads/:id` and `POST /api/uploads/:id/process` each match
   the upload's status in their own filter; a completed row answers the same 404/403 as a wrong
   secret. `GET` keeps working for the recipient's page. `src/lib/uploads/secretAuth.ts`,
   `tests/upload/secretAuth.test.ts`.
8. **Project names and slugs are unique among live projects only.** A soft-deleted project no
   longer holds its name (the old index answered 409 for a project nobody could see).
   `db/migration/20260925_0003`, `src/lib/models/Project.ts`.
9. **AI run records expire after 30 days** (`AI_RUN_RETENTION_DAYS`). They hold prompt text built
   from customers' documents and were kept forever. Existing rows older than the retention are
   removed when the index builds. `src/lib/models/AiRun.ts`, `db/migration/20260925_0004`.
10. **`analytics-reconcile` is bounded.** The nightly counter pass covers links with activity in
    the last two days (`?days=N`; `?full=1` for everything) and holds the cron lease; it used to
    scan `shareviews` and `sharevisits` end to end (review M9). `?dryRun=1` on any cron no longer
    overwrites the job's health record (M12, `src/lib/cron/health.ts`). `usage-agg-reconcile`
    recomputes each billing cycle over all its rows, so a one-day run no longer replaces a cycle
    total with one day's spend (H6, `src/lib/usage/reconcile.ts`).
11. **Deleting a workspace or an account now touches Stripe.** `DELETE /api/orgs/:orgId` cancels
    the workspace's subscription immediately (prorated on yearly) before deleting anything and
    refuses with 502 `STRIPE_CANCEL_FAILED` if Stripe cannot be reached; `POST /api/account/delete`
    sets cancel-at-period-end on every subscription nobody else could manage (and refuses the same
    way), and where another owner or admin remains posts a `plan.subscription_ending` row asking
    them to replace the card. The 30-day purge skips a subscription already set to end, so a
    prepaid year is never forfeited. Review H2, M1; `src/lib/billing/stripeSubscriptionCancel.ts`,
    `src/app/api/account/delete/route.ts`, `src/app/api/orgs/[orgId]/route.ts`,
    `src/lib/accounts/purge.ts`, `docs/SUBSCRIPTION.md`.
12. **`/api/debug` and `/api/debug/cookie` are admin-only** through `requireAdmin` (404 to anyone
    else in production). A preview host used to hand a session cookie back to any signed-in user.
    `tests/lib/debugRoutesAdmin.test.ts`.
13. **Contained documents are absent from every workspace-wide list.** New field
    `Doc.visibility: "workspace" | "project"`; nothing existing changes (the default is
    `workspace`), but any listing built outside the shared filter would miss contained documents,
    and `tests/lib/containedDocListings.test.ts` pins every listing to it. Details under owners.
14. **A locked data room answers 401 for every document id** on
    `POST /api/share/:shareId/download-requests` and on the stats route; it used to answer 404 or
    401 by membership, which leaked which ids were inside (review M8, `docs/SECURITY.md` 7.8).
15. **The activity feed hides instrumentation rows** (`funnel.*`, `checkout.started`) and the
    feature-gate refusals (`plan.limit_reached` on `version_history`, `analytics_history`,
    `project_links`) unless a `type=` filter names them; the counted caps stay visible.
    `plan.limit_reached` is written once per workspace and limit per ten minutes, so the admin
    overview counts slightly fewer rows on hot paths. `src/lib/activity/feedVisibility.ts`,
    `planLimitResponse` in `src/lib/billing/planLimits.ts`.
16. **Stripe webhook, yearly subscriptions.** When the licensed item is yearly the handler nulls
    `stripeSubscriptionItemId` and turns on-demand off, so a row switched to yearly in the Stripe
    dashboard stops allocating credits no metered item bills. No new webhook events.
17. Smaller ones: `User.tempSecretHash` is `select: false` (every read must ask for it);
    `GET /api/docs/:id/changes` is read-only for viewers and API keys; `GET /api/admin/data/requests`
    no longer runs a backfill on every load (it moved to migration `20260925_0005`); the MCP's
    `get_share_link_password` refusal no longer blames "an API key" when the caller signed in over
    OAuth (`api_key_forbidden` code unchanged); `activityevents` reads for the funnel use a new
    `{ type, createdDate }` index.

## For owners and members

**Slack integration** (new; `docs/prds/lnkdrp-slack.md`, help at `/help/slack`). **Gated by scale,
not as a feature**: Free connects one channel and it posts all five kinds of message; the second
channel, and routing a project to its own channel, are Pro. See "Slack is priced by amount, not by
feature" at the end.
**Integrations** is a new sidebar entry; `/integrations/slack` connects channels with Slack's own
install screen (`incoming-webhook` scope, private channels work). Five moments post: a recipient's
first open (and their introduction), the visit brief, a replaced document with its change
summary or a new share link, a file received in a request inbox, and a new document (finished
uploading, or added to a data room). Each channel has five switches, "Send a test message" and
"Disconnect"; the first channel is the default and projects can be routed to their own channel
(one project posts to one channel). Reader identity follows the plan as in the emails: Pro names
the reader, Free reads "Someone". Posts are queued in `SlackOutbox`, sent at the moment, retried by
the `notification-emails` cron on the email backoff ladder, capped at 30 a minute per channel with
one "and N more" line, and a webhook Slack reports gone marks the channel Disconnected. Webhook
URLs are stored encrypted. Off entirely until the deployment has `SLACK_CLIENT_ID` and
`SLACK_CLIENT_SECRET` (the page says so). Activity rows `integration.slack_connected` and
`integration.slack_disconnected`. `src/lib/slack/*`, `src/app/(app)/integrations/*`,
`src/app/api/slack/*`, `src/app/api/orgs/active/slack/*`.

**A document's home project, and staying inside it** (`docs/prds/lnkdrp-project-home.md`).
- Upload into a data room: the upload page has an "Add to a data room" picker (preselected by
  `/upload?project=<id>`, where a project page's new "Upload here" button goes); `POST /api/docs`
  takes `projectId`. The document is in the room from its first write, so there is one creation
  event ("... in Data room" in the feed, one Slack post to the room's channel). A request inbox is
  refused (400 `PROJECT_IS_INBOX`).
- Contained documents: "Only inside this data room" at upload, or "Keep inside its data room" from
  the document menu, keeps a document out of the Docs sidebar, `/api/docs`, search, tags, starred,
  recent changes, the dashboard, workspace metrics and suggested documents; its activity rows show
  under the project's feed; Slack routes it to the room's channel or nowhere. Its direct link,
  metrics, history and links keep working. A contained document refuses a second project
  (409 `CONTAINED`). The document page shows a **Contained** pill; `doc.contained` and
  `doc.uncontained` feed rows. `src/lib/docs/visibility.ts`.

**A data-room reader is named everywhere.** A reader who introduces themselves on a room's landing
page was "Someone" in visit briefs, Slack posts and on the reader page while the feed named them.
One helper (`src/lib/share/readerIdentity.ts`) reads the arrival row and the share views the way
the feed does; the brief row stores the name; the introduction itself posts to Slack as its own
line, on Free too.

**Analytics on Free show real numbers instead of blurred rows.** The metrics page's locked block now
says "N named people and M anonymous readers opened this since <date>" with one Upgrade to Pro
button, computed over the document's whole life (`src/lib/analytics/teaser.ts`; the document and
project share-views routes send `teaser` on the basic tier). Identities were always recorded on
Free and are revealed on upgrade; the teaser says how many there are. The reader page on Free
shows the upgrade block instead of "No reader by that id".

**Every credit and plan wall points at the door that opens.** The out-of-credits modal, the blocked
banner, the sidebar credits row and the Overview credits card offer the cheapest credit pack first
on Free and Pro second; yearly Pro (no on-demand) is routed to `/credits` for packs. The
team-workspace cap and the visit-brief wall open the upgrade modal instead of red text. The
`/credits` "Or get Pro" card offers the Monthly / Yearly choice (hidden once on Pro). The home
upload notice shows the grace countdown; document-cap copy reads the real constant. Commit
`4c7e571`, `99a7d2c`.

**Pages paint from what the tab already knows.** Metrics, Activity, Search, Requests and Tags show
their last answer at once and refresh underneath; empty states render on the first frame when the
sidebar snapshot already says there is nothing (`src/lib/client/pageCache.ts`, `knownEmpty.ts`);
Activity paints "No activity yet" from a new `activity.any` flag on the sidebar snapshot. The
Integrations page resolves its state on the server so "Set up" never flashes over a connected
channel.

**Fixes found in the review and the data-room drive.**
- A failed replacement puts the document back on its last good version instead of leaving it in
  `preparing` behind a failed upload; one routine serves the processing job, the import abandon
  path and the read-time repair (M5, `src/lib/uploads/restoreDocAfterFailure.ts`). A failed
  "Import from a link" deletes the "Untitled document" it created, from the client and, if the
  tab closed, from the server (M31).
- The owner's PDF viewer could pin the previous version's bytes for a year: `?v=<uploadId>` now
  redirects to that upload's own blob, cacheable only once it exists (`d369bad`,
  `tests/lib/ownerPdfCacheKey.test.ts`).
- AI compare failed on one over-long sentence after charging; the model-facing schema no longer
  carries the storage caps and the answer is clipped afterwards (`src/lib/ai/docChangeDiff.ts`).
- Visit briefs are charged one credit each and reach the feed, the email and Slack (verified in
  the ledger); a schema-shaped interest no longer renders as "string · the line" (`3ce2597`).
- The History chip (`#v-N`) no longer locks the whole app behind an overlay; the overlay compares
  pathnames and releases on hashchange, popstate or after 15 s (H7, `src/lib/client/navPathOf.ts`).
- `copy_to_new_project` in request repos always failed after creating the new project (a
  self-conflicting Mongo update); it works, and removes the new project again if the loop fails (H5).
- Invoices load on a cold visit to the Billing tab (H9). The auth gate refreshes the next-auth
  session and keeps polling instead of spinning forever (M22). A silent metrics refresh keeps the
  viewer lists (M23). The client-side first-page thumbnail renders again on upload, replace and
  request pages (M25, `src/lib/client/pdfThumbnail.ts`). Markdown inline code renders inline (M21).
  History paging ignores superseded queries (M26); project settings keep the guide-upload modal
  open until the guide flow completes (M27); the project docs list has an error state (M28); the
  account menu stops refetching on every focus (M30); the all-pages viewer only cancels renders
  outside the new set (M33); a forced sidebar refresh cannot resurrect a just-deleted document (M34).
- Two people redeeming invites at once could exceed the collaborator cap; the claim re-counts and
  rolls back (M4). Two requests bootstrapping a new account at once no longer throw E11000; the
  loser joins the winner's workspace (`src/lib/models/Org.ts`). The workspace cap is re-counted
  after create. `Project.docCount` is recomputed rather than incremented, so it cannot drift.
- Every stored blob URL passes the store allow-list before the app hands it to a browser; thirteen
  document routes use `buildDocMatch`; workspace-scoped lookups in `POST /api/uploads` and
  `GET /api/uploads/:id` (review Low, docs/uploads).
- Support chat: the widget's type matches the app and the heading override reaches the shadow
  root; a signed-in person's thread carries the workspace and plan so the support agent answers
  for the right plan.
- Help articles: `/help/slack` (new), `/help/connect-your-agent` leads with signing in,
  `/help/teams-and-workspaces` says "workspace" throughout.
- Contacts (new): `/contacts` is the people a workspace has heard from, gathered from the four
  moments that produce an address: an introduction on a landing page, a signed-in read, a download
  request, and a request-inbox upload. The address is the identity and is folded before it is
  looked up, so one person is one row however they typed it; a webmail domain never becomes a
  company. The list searches, filters by tag, document, project, link, domain and source, sorts,
  and exports to CSV, and a contact's own page shows what they read and when. On Free a contact
  who never introduced themselves is a domain and dates without a name or an address; an
  introduction is theirs to give, so those rows keep their identity on every plan. Everyone the
  workspace had already heard from is backfilled once by
  `db/migration/20260925_0006_contacts_backfill.mjs`, so the page is full on day one. Agents can
  read the same list over MCP with `lnkdrp_list_contacts` and `lnkdrp_get_contact`.

## For recipients

- Share pages: a link that is refused or locked no longer advertises an `og:image` that 404s;
  the share cookie is compared in constant time (`src/lib/share/cookieCompare.ts`); a locked data
  room answers the same 401 for every document id; a download request is deduplicated per
  document; room request emails link to the room; `page-image` sniffs its content type; `og.png`
  reads three fields; `/s` and `/p` pages resolve the link once per request; room thumbnails carry
  an ETag. (review Low, public share; `tests/lib/shareCookieCompare.test.ts`,
  `roomDisclosure.test.ts`, `downloadRequestDedupe.test.ts`.)
- A reader who introduces themselves on a data-room landing page is named on every later open and
  brief the owner sees, and on the room's reader page.
- Recipients uploading through a request or replace link keep their progress page, but cannot
  change a version after it has completed (change 7 above).
- A contained document's link opens exactly as before; containment is invisible from outside.

## For agents over MCP

- **Connect by signing in (OAuth 2.1), keys unchanged.** Any client that implements MCP
  authorization (Claude Code, Cursor, Codex CLI, Gemini CLI, the Claude.ai and ChatGPT
  connectors) adds `https://mcp.lnkdrp.com/mcp` with no key: the MCP's resource metadata names the
  app as authorization server, the app registers the client (RFC 7591, `POST /api/oauth/register`,
  20 an hour per address), the person picks a workspace and clicks Allow at `/connect/authorize`,
  and the client exchanges a PKCE-bound code for an `lnko_` access token (1 hour) and an `lnkr_`
  refresh token (30 days). `verifyBearer` resolves the token like a key to the same `Actor`, so
  every tool, gate, plan limit and activity row is unchanged; the MCP binds the session to the
  grant id (`credentialId`), so the hourly refresh keeps the session. Grants list on `/connect`
  beside keys as "Signed in" with one Revoke (`DELETE /api/agent/keys/:id` takes either id;
  `POST /api/oauth/revoke` too). A viewer's grant is read-only. The `/mcp/<client>` guides and
  `/connect` lead with signing in; Grok keeps the key steps. `src/lib/agents/oauth.ts`,
  `oauthAuthorize.ts`, `oauthHttp.ts`, models `OAuthClient`, `OAuthCode`, `OAuthGrant`,
  `src/app/api/oauth/*`, `src/app/.well-known/oauth-authorization-server/route.ts`,
  `docs/MCP.md` "Signing in instead of a key", `docs/prds/lnkdrp-mcp-oauth.md`. Not yet run
  against a live client.
- **Four new tools, and two more for contacts: 39 in all** (`docs/MCP.md`; `EXPECTED_TOOLS` in
  `tests/mcp/e2e.ts` is the list the harness enforces). The four:
  - `lnkdrp_list_revisions { docId? | shareId?, since?, limit?, cursor? }`: every replacement's
    change record, newest first, workspace-wide or for one document, `since` as an ISO date or
    `24h`, `7d`, `30d`, `this_week`, `this_month`; keyset-paginated.
  - `lnkdrp_get_revision { docId? | shareId?, version?, includeText? }`: the full compare for one
    version (summary, itemised changes, per-page change kind with previous and new wording, file
    sizes and page counts, the compare's own state, optionally both versions' text).
  - `lnkdrp_revision_contributors { docId? | shareId?, since? }`: who makes the changes, per member
    and per agent client, most active first.
  - `lnkdrp_set_doc_visibility { docId, visibility }`: keep a document inside its data room or list
    it in the workspace again; `VISIBILITY_NEEDS_PROJECT` (400) when it is in no project.
  Behind them: `GET /api/changes` (workspace-wide, `?since=`, `?docId=`, `?contributors=1`,
  cursor) and `?version=N` on `GET /api/docs/:id/changes`; `PATCH /api/docs/:id { visibility }`.
- `lnkdrp_share_pdf` takes `projectId` or `projectSlug` (at most one) and creates the document
  inside that room from the first write; the project is resolved before anything is created.
  `lnkdrp_list_docs` and `lnkdrp_get_share` carry `primaryProjectId` and `visibility`;
  `lnkdrp_list_docs` leaves contained documents out (find them through `lnkdrp_get_project`);
  `lnkdrp_add_docs_to_project` reports refused contained documents under `contained`.
- `lnkdrp_whoami` (and `GET /api/agent/whoami`) reports `credentialId`, `credentialKind`
  (`key` | `oauth`) and `integrations.slack` (channels, default, routing, switches; never the
  webhook, and no tool writes it). `keyPrefix` reads "signed in" for a grant.
- The confirmation policy (change 3), inline limits (4), session caps and per-credential
  idempotency (5) above. `/healthz` now reports `sessionCaps` and `confirmations: "enforced"`.
- CORS on `/mcp` is an origin allow-list (`MCP_CORS_ORIGINS`), off by default; the OAuth endpoints
  and metadata documents are CORS-open.
- `lnkdrp_get_activity` accepts the new types (`funnel.*`, `checkout.started`, `doc.contained`,
  `doc.uncontained`, `integration.slack_*`, `agent.authorized`, `agent.disconnected`,
  `share.visit_briefed`).
- `lnkdrp_list_contacts` and `lnkdrp_get_contact` are the other two of the 39, read-only, described
  under Contacts above.
- The e2e harness (`tests/mcp/e2e.ts`) survives a Free workspace, asserts the revision tools and
  cleans up its tags; the 2026-09-24 runs are recorded in `docs/reviews/mcp-test-run-2026-09-24.md`
  (58/58 on Pro over OAuth, 50/50 on Free). Its `EXPECTED_TOOLS` list now has 39 entries and the run
  fails if the server exposes a tool that is not on it.

## For admins

- **`/a/funnel`** (Billing section, `GET /api/admin/funnel?weeks=N`, `src/lib/funnel/report.ts`):
  per ISO week, distinct workspaces at each step of the upgrade funnel (sign-ups, first wall, any
  wall, prompt shown, each CTA, teaser shown with the median readers it showed, Checkout started
  for Pro or a pack, upgraded), plus the median days to the first wall and which limit fires
  first. The rows come from `plan.limit_reached` (written centrally by `planLimitResponse`, feature
  gates included), `funnel.modal_shown` / `funnel.cta_clicked` / `funnel.teaser_shown`
  (`POST /api/funnel` from the browser) and `checkout.started` (Stripe checkout and credit-pack
  purchase). `docs/reviews/pricing-upsell-fix-plan-2026-09-23.md` Phase 4.
- `/a/shareviews/<docId>` is paged (`?limit` default 100, cap 500, keyset cursor) with "Load older
  views" (M13). The admin credits page builds the usage cycle key the way the usage writer does,
  so cycle totals are no longer 0 (M10, `src/lib/credits/cycleKey.ts`). `/a/deletions` and the
  member counts use one aggregate each. Plain support cards are read-only.
- Errors: `errorJson` logs every unhandled exception and the twelve catch-alls answer 500 instead
  of 400 (M18); stack traces are redacted like messages, at write time and again for older rows in
  `/a/errors` (M19).
- Crons: `?dryRun=1` no longer writes health (M12); `analytics-reconcile` takes the lease and is
  bounded (M9); `account-purge?userId=` asks for that one account instead of the first thousand.
  No new cron; `vercel.json` unchanged.
- Preflight (`/a/env`): `SLACK_CLIENT_ID` and `SLACK_CLIENT_SECRET` as wanted rows; a multi-host
  Mongo URI no longer reports a false failure; the probe opens the database the app will.
- Debug routes behind `requireAdmin` (change 12).
- CI: `.github/workflows/test.yml` runs `tsc`, lint and `npm test` on every pull request and push
  to `main`; `npm test` is the three unit suites (`docs/TESTS.md`).
- Operations: the realtime server reads `REALTIME_MONGODB_URI` (falling back to `MONGODB_URI`) and
  projects its `projects` change stream so request-repo capability tokens stop travelling to that
  host (M20); the MCP calls `https://www.lnkdrp.com`, not the apex that 308s
  (`deploy/fly/mcp.fly.toml`); dev twins `dev-lnkdrp-realtime` and `dev-lnkdrp-mcp` exist on Fly
  (`deploy/fly/dev-*.fly.toml`, `COMMANDS.md`); `scripts/realtime-e2e.ts` proves a deployed
  realtime server end to end. `PRODUCTION.md` records realtime as live and Slack as not configured.
- Docs: `DEPLOY.md` 4.7 (Slack app), the `REALTIME_MONGODB_URI` rows, and the branch rule in 9;
  `docs/MCP.md` (OAuth, revisions, limits, caps, deployment status); `docs/SECURITY.md`,
  `docs/SUBSCRIPTION.md`, `docs/CRON.md`, `docs/TESTS.md`, `docs/FEATURES.md`; PRDs for Slack,
  a document's home project and contacts; reviews: the full code review of 2026-09-23 with its
  findings marked closed, the pricing analysis and fix plan, the overlap audit, the MCP test run,
  the naming assessment and the session summary. Four `scripts/_tmp-*.ts` data-room drive scripts
  are committed with the branch.

## Added after the first draft (2026-09-26)

Fifteen commits landed after these notes were first written against `7f98f91`. What a person
notices, by who notices it.

**Every contributor has a page.** `/people/<userId>` and `/agents/<client>/<ownerUserId>`: one
person or one agent, and everything they have done in this workspace. The bold subject name on any
activity row is now a link to it. Each page carries all-time tiles (first activity, last activity,
Actions, the five summary buckets and Other work), the by-day work chart the Activity page draws,
a right rail of the agents they connected, the documents and the projects they touched, and the
ordinary feed underneath with the same tabs and paging. A deleted document keeps its rail row,
struck through and unlinked, because the work happened and dropping it would make the totals
disagree with the list.

- **An agent is "this client, connected by this person".** Two members who each connect Claude Code
  are two contributors with two pages (`agent:<client>@<ownerUserId>`). An agent's page says
  "Connected by <name>" and links back; a person's page lists the agents they connected. A
  tombstoned owner reads "a member who has left", no owner at all reads "an unknown member".
- **A row credited to two names is two links.** "Ana Lima and Claude Code" used to be a single link
  to the agent, so clicking the person's name on that agent's own page did nothing, which is what a
  broken link looks like. Each name now goes to its own contributor. The split only ever applies to
  the exact sentence the label builder writes for a co-credited row, so a document called "Tom and
  Jerry" is never cut in half, and a recipient's row is never split, because a reader is not a
  contributor.
- A row an agent wrote opens the agent's page even when the sentence prints the member's name (as
  `doc.replaced` does), and the tooltip says where it goes ("See everything Claude Code changed"),
  so a person's name never silently opens an agent's page.
- Following "Connected by" off an agent's page now shows its own loading state instead of leaving
  the agent's name, tiles and chart on screen under the person's URL, and an error if the next
  profile fails. The feed and the rail also start on the same line.
- Data: `GET /api/activity/actor?key=<contributor key>` for the profile, and `actor=<key>` on
  `GET /api/activity` and `GET /api/activity/summary` for the feed and the chart. The filter
  excludes recipient and secret rows by construction, so it can never become a way to read a
  reader's history; tenancy comes from the session; a key with no activity in the active workspace
  answers 404, so a foreign id cannot be used to probe. On Free the identity gate blanks the key and
  the link along with the name. Over MCP, `lnkdrp_revision_contributors` now carries a `url` so an
  agent can hand a person the same page.

**Slack is priced by amount, not by feature.** Free connects one channel (`FREE_SLACK_CHANNELS`) and
it posts all five kinds of message; the second channel and routing a project to its own channel are
Pro, the same shape as documents and projects. The cap is enforced in the OAuth callback rather than
at the button, because the install is Slack's own screen and the callback is reachable from a link
or a stale tab: past the cap nothing is stored and the page says why. Reconnecting a channel the
workspace already has is an update, never a new channel; a channel Slack revoked does not count.
Dropping to Free never traps existing routing, because an empty `projectIds` is always allowed. The
page shows a Pro pill on Add channel and on the routing picker rather than a link that dies at
Slack's screen, and the upsell names what it buys ("Routing a data room to its own channel is a Pro
feature"), not the number it refuses. Both pricing columns mention Slack. **Contacts, by the same
argument, stays on every plan**: a Free list showing forty domains with the names withheld argues
for Pro better than a locked page does.

**The Slack channel reads as a channel now.** Blocks travel inside an attachment with a coloured
left bar: the accent green for anything a recipient did (opened, introduced themselves, finished
reading, sent a file to a request inbox), grey for what the workspace did to its own documents
(added, replaced, a new link). Each kind leads with one emoji, and a project is marked wherever it
is named, a folder for a data room and a tray for a request inbox, which is the one distinction the
app itself draws. The emoji are in the blocks only, so the plain-text twin Slack reads for mobile
notifications and screen readers stays clean. Link previews are refused on every post, since every
URL points into the signed-in app.

- **Posts arrived twice and no longer do.** `text` beside an attachment is not a fallback: Slack
  renders it as the message and hangs the attachment underneath. It moved inside as `fallback`.
- **`/integrations` stopped flashing "Set up" over a connected channel.** The page resolves Slack on
  the server, and that read was returning null every time: NextAuth's `getToken` finds the session
  cookie on `req.cookies` only and never parses the cookie header, and a server component's
  synthesized Request has no jar, so a signed-in owner read as signed out, silently.
  `serverComponentRequest` attaches it.
- The homepage gains a Slack shot, composed rather than photographed: the settings half is a
  screenshot and the channel half is drawn from the same marks the renderer posts with, so the
  picture cannot quietly disagree with the product. It replaces a real capture whose bold last line
  read "spent 30 sec on the lorem ipsum text" and whose reader wore a real firm's name.

**Contacts: the CSV tells the truth about its own size.** The export built the whole file in memory
and stopped at ten thousand rows without telling anyone, so a large workspace got a file quietly
missing people. It now streams from an async generator in batches of 500, the cap is 25,000, and the
count runs first: past the cap the route answers 413 with the number and asks for a narrower list,
and the page's button reads "Too many to download" instead of offering a link that would fail. That
check has to come first, because a download is a 200 the moment it starts and there is no way to
tell it later that it is short. A UTF-8 BOM leads the file, so Excel reads an accented name as a
name rather than mojibake.

**For admins: a seventh migration.** `20260925_0007_activity_actor_index.mjs` builds the keyset index
the contributor pages page with. Two older `activityevents` indexes come off by hand after the
deploy, in order, because each is now a prefix of a wider one and every write pays for both. Both
are in `docs/deploy/checklist-2026-09-25.md` steps 1 and 3.

**One thing that is in the commits but not in this release.** `fbede1d`'s message says a locked data
room no longer falls through to the catch-all Slack channel. The routing code for it is committed,
but the `Project.visibility` field it reads is not: it belongs to the locked-projects work still in
progress. On this release the query matches nothing and routing behaves exactly as it did before, so
there is nothing new to announce and nothing to leak, since no room can be locked yet. It becomes
true when locked projects land.

## What the review left open

From `docs/reviews/full-code-review-2026-09-23.md` after this release: M35 (realtime probes each
stream before `listen`; the code-13 exit shipped earlier on `main`), the plaintext capability tokens
on `Project` and `Upload` at rest (M20 is closed on the wire only), `select: false` on the share
password fields, the legacy pay-as-you-go flip, the MCP slug scan, and sixteen files outside
`docs/**` still to convert to `buildDocMatch`. Pricing plan Phases 3 and 5 (seat SKU, the
credit-rate decision, trial) are decisions, not code.
