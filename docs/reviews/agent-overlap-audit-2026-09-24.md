# Agent B overlap and quality report (2026-09-24)

Premise correction up front: almost none of Agent B's work is uncommitted. `git status` shows only 11 modified files plus Agent A's untracked ones; Agent B's real output is five commits on `next-release` (`110d23a` db diagnostics, `26972f6` scripts exit, `6f61fd2` sidebar, `a62c45a` em-dash sweep, `bb19104` yearly Pro, `e6562bd` COMMANDS.md). Commit `bb19104` also swept in Agent A's Phase 0 hunks for `src/app/api/stripe/checkout/route.ts` (4 `hasOpenSubscription` lines), `webhook/route.ts`, `subscriptionState.ts` and `docs/SUBSCRIPTION.md`; `a62c45a` carried the `labels.ts` `plan.subscription_ending` case. Those files have no working-tree diff any more. Agent A's still-uncommitted set is: `account/delete/route.ts`, `orgs/[orgId]/route.ts`, `BillingInvoicesTab.tsx`, `activity/log.ts`, `mcp/src/tools/discover.ts`, `package.json`, `docs/TESTS.md`, and the untracked `stripeSubscriptionCancel.ts`, `stripeWebhook.test.ts`, `.github/workflows/test.yml`, three `docs/reviews/*.md`. The plan's "Phase 0: done, uncommitted" line (fix-plan:13) is half stale.

## 1. What Agent B has been doing

Agent B shipped one product feature and a set of infrastructure and style sweeps. The feature is annual Pro billing (`bb19104`): `Subscription.interval`, a Monthly/Yearly toggle on `/pricing`, the UpgradeModal and the Plan card, `POST /api/stripe/checkout` accepting `interval: "year"` and selling `STRIPE_PRICE_ID_ANNUAL` without the metered item, the webhook deriving `interval` from the licensed item, `onDemandEligible` (monthly Pro only) and `creditPacksAllowed` (Free or annual Pro) gating spend, purchase, status and snapshot, annual price labels in `BillingConfig` via the admin refresh, preflight checks, env/runbook docs and `tests/lib/annualBilling.test.ts` (9 cases). Around it: a repo-wide em-dash sweep of user-facing strings enforced by a new AST test (`tests/lib/noEmDashes.test.ts`), a Mongo access-diagnostics module (`src/lib/db/access.ts`) wired into mongoose, the realtime server (fatal boot check, fast exit on code 13) and preflight, a Windows-safe `exit()` helper for five scripts, a Windows path fix in `imageImportClosure.test.ts`, a sidebar scrollbar tweak, `COMMANDS.md`, and four small uncommitted leftovers (`.gitignore` append, `PlainChat.tsx` `hideBranding`, a `visitBriefs.ts` comment, a `BillingInvoicesTab.tsx` placeholder glyph). `tsc --noEmit` is clean and all touched suites pass (annualBilling 9/9, stripeWebhook 10/10, mongoAccess, admin suites), but `node scripts/build-index.mjs --check` fails, so `npm test` is red on this tree.

## 2. Overlap with the plan and the code review

| Plan/review id | Agent B's change | Verdict | Acceptance met | Issues |
|---|---|---|---|---|
| 3.1 | Checkout `interval` param, annual line item, `ANNUAL_NOT_CONFIGURED` (`checkout/route.ts:120-146, 200-228`) | good code, issues around it | partial: code path exists; prod env var, admin refresh and FAQ bullet undone | 5 (serviceCore gate, stale item id, FAQ, SUBSCRIPTION.md:35, source-grep tests) |
| 3.1 | `Subscription.interval` field and `subscriptionState.ts` helpers | good, issues | partial | 3 (FAQ, `:10` "300" comment, hot-reload block `Subscription.ts:92`) |
| 3.1 | Webhook derives/persists `interval`, annual price read as Pro | good, issues | partial (integration verifier said yes on the Accept line by code; correctness verifier said partial because untested and env unset) | 4 (serviceCore, no annual webhook test, DEPLOY.md:411, invoice.paid side door) |
| 3.1 + 3.3 (different resolution) | spend/status/purchase/snapshot honour annual | issues | partial: 3.3 Accept met for annual Pro only | 6 (serviceCore medium, `isPro` field lies, 409 copy, SUBSCRIPTION.md:73, creditsAdmin.ts:300, `$39` comment) |
| 3.1 | `startCheckout({ interval })` | good | n/a. Verifiers disagree: correctness says not-an-overlap (committed, sound); integration filed issues, all in other files (purge.ts:277, PricingCta:166). Prefer correctness for the file itself; the purge finding stands and is in section 3 | 1 in-file (docblock `:54`) |
| 3.1 | `tests/lib/annualBilling.test.ts` | good, issues | partial | 3 (no route-level interval test, verbatim 130-char match `:112`, misleading case title `:111`) |
| 3.1 | `/pricing` toggle: `BillingInterval.tsx`, `page.tsx`, `PricingCta.tsx` | good, issues | partial (FAQ bullet not done) | 5 (FAQ x3, toggle shown to monthly Pro, SignedOutCta drops choice, hard-coded "2 months free") |
| 3.1 (adjacent) | UpgradeModal yearly option | good | n/a. Correctness says not-an-overlap, integration says good; no disagreement of substance | 4 low (yearly state persists across opens, keyboard a11y, FEATURES.md:350, no test) |
| 3.3 (partial) | `CreditsPurchaseClient.tsx` packs for annual Pro | issues | partial: annual Pro only, monthly Pro 409 kept, inversion remains | 5 (false "AI stops" caption `:416`, CreditsSummaryCard dead end, hidden workspace note `:437`, stale header `:10`, credits/page.tsx:69) |
| M35 | Realtime fatal boot check, 5 s exit on code 13 | issues | partial: exit-on-13-named done; no per-stream probe, no 40573, wrong-DB explanation for missing changeStream. Correctness says issues, integration says not-an-overlap (no A files touched); both agree on the gaps | 3 low |
| Low: preflight multi-host URI | Preflight code-13 explanation | broken for the claimed reference | no: `env.ts:172` still uses `new URL`, false FAIL on comma hosts | 3 (one-line fix available) |
| 3.1 (tooling) | Preflight `STRIPE_PRICE_ID_ANNUAL` row and shape check | good | no/partial: helps bullet 1, performs none of 3.1 | 3 (preflight vs BillingConfig source of truth, one-sided shape rule, no test) |
| 3.1 | Annual docs: `.env.example`, DEV.md, DEPLOY.md, PRODUCTION.md, MCP.md | issues | partial | 6 (DEPLOY.md 300 credits, switch procedure hits 409, wrong xref 5.4, retired packs, env matrix, usage_type claim) |

Excluded as not-an-overlap by both verifiers: `SubscriptionCard.tsx` (issues moved to section 4), the two lifecycle routes (Agent A's hunks only), `scripts/lib/exit.ts`, `imageImportClosure.test.ts`, `.gitignore`, `COMMANDS.md`, em-dash sweeps, ADMIN_DASH switch. The `BillingConfig` annual-labels change (mapped as Phase 6 adjacent) was not verified.

## 3. Conflicts with Phase 0

None of Agent B's hunks break Agent A's logic; `stripeWebhook.test.ts` passes 10/10 on the merged tree and the interval write sits inside the same ordered `updateOne` Agent A wrapped in the 11000 retry. The composition problems are semantic:

1. **Annual forfeiture on delete** (`src/lib/accounts/purge.ts:277-300`, `src/app/api/orgs/[orgId]/route.ts:179`, `src/lib/billing/stripeSubscriptionCancel.ts:51-55`). Agent A's 0.2 sets `cancel_at_period_end`, then the 30-day purge hard-`DELETE`s solo workspaces' subscriptions; 0.1 cancels immediately with no `prorate`. Written for $29/month, now reachable for a $290/year row: up to eleven prepaid months forfeited with no credit. Fix: for `interval === "year"` either skip the purge hard-cancel when `cancelAtPeriodEnd` is already set, or cancel with `{ prorate: true }`; on org delete read `interval` and either schedule period-end or prorate; add one sentence to the delete confirm dialogs and to `docs/SUBSCRIPTION.md` "Cancelling from inside the product".
2. **Monthly-to-yearly switch is a dead end** (`src/app/pricing/BillingInterval.tsx:68`, `DEPLOY.md:410-411`, `checkout/route.ts:171`). The toggle renders for a signed-in monthly Pro; Agent A's `hasOpenSubscription` guard answers 409 while the subscription is `active` even with cancel-at-period-end set, so the documented "cancel at period end, then the other Checkout" fails until the period ends. Fix: hide or disable the toggle when billing status is `plan: "pro"`; reword DEPLOY.md step 5 to "cancel immediately with proration, wait for `customer.subscription.deleted`, then Checkout".
3. **Dead 409 branch** (`src/app/pricing/PricingCta.tsx:165-166`, `src/lib/billing/clientActions.ts:54`). Agent A's 409 now carries `redirectTo` and `failOrRedirect` navigates instead of throwing, so B's `/already/i` catch never runs and the docblock still says "billable". Fix: delete the catch, reword the docblock.
4. **0.5 acceptance weakened** (`webhook/route.ts:314-331`). `restoreFreeDailyCap` skips rows with `purchasedCreditsRemaining > 0`; annual Pro can now buy packs, so a lapsed annual Pro holding pack credits keeps the cap lifted. Consistent with the documented pack rule, but the Accept line "cap back at 15" no longer holds for that case. Fix: note it in `docs/SUBSCRIPTION.md:51` or narrow the carve-out.
5. **HEAD references untracked files** (`docs/SUBSCRIPTION.md:52, 56`, committed in `bb19104`): names `tests/lib/stripeWebhook.test.ts` and `src/lib/billing/stripeSubscriptionCancel.ts`, both still untracked. Fix: commit the remaining Phase 0 files now.
6. **Sweep leftovers inside Agent A's files** (`src/app/dashboard/BillingInvoicesTab.tsx:1177` glyph change, `src/lib/visits/visitBriefs.ts:971` comment). Whoever commits 0.6 carries B's hunk. Fix: commit them as a separate `copy:` commit first, or revert `visitBriefs.ts` (the comment now contradicts the regex on `:974`).

## 4. Issues to fix in Agent B's work

Most severe first; all confirmed by a verifier with file:line evidence (items 1, 2, 8 and 12 re-checked by me).

1. **`INDEX.md` stale, `npm test` red** (`INDEX.md`, `scripts/build-index.mjs --check` exits 1). Three tracked files unindexed: `src/app/pricing/BillingInterval.tsx`, `src/lib/db/access.ts`, `scripts/lib/exit.ts`. `tests/lib/indexMap.test.ts` runs that check, so Agent A's `npm test` and CI cannot be green. Fix: `git add` the untracked Phase 0 files, `npm run index`, commit `INDEX.md` with Phase 0.
2. **Uninvoiced on-demand on annual Pro** (`src/lib/credits/serviceCore.ts:17-21`, used as `isOnDemandEligible` at `:187`). `defaultIsProWorkspace` selects `{status, kind}` and returns `isProSubscription`; spend/snapshot use `onDemandEligible`. A row switched to yearly (Stripe dashboard item swap) with stored `onDemandEnabled: true` keeps allocating `on_demand` credits that no metered item bills; `webhook/route.ts:545-546` also keeps the stale `stripeSubscriptionItemId`. Fix: select `interval` and return `onDemandEligible(sub)`; in the `updated` handler call `disableOnDemandForSubscription` and null `stripeSubscriptionItemId` when `interval === "year"`.
3. **Pricing page contradicts the annual rule** (`src/app/pricing/page.tsx:258, 392, 396`). "On Pro, turn on on-demand" with no yearly caveat, on the page that sells yearly. This is fix-plan 3.1 bullet 3. Fix: "on monthly Pro turn on on-demand; on yearly Pro buy a credit pack", gated on `annualAvailable`.
4. **Annual Pro routed to a dead end on Overview** (`src/app/dashboard/CreditsSummaryCard.tsx:256-265, 286-290`, `dashboardShell.tsx:310`). Links to `/dashboard?tab=limits`, where spend refuses yearly; never points at `/credits`. Fix: expose `interval`/`creditPacksAllowed` in the snapshot payload and render the Free-style pack prompt for annual Pro.
5. **False caption on `/credits` for annual Pro** (`src/app/credits/CreditsPurchaseClient.tsx:416-427`). "Off. AI stops when included credits run out" above the packs it sells; also the workspace note at `:437` and eyebrow `:378` hidden by `isPro`. Fix: branch on `showPacks`, give annual Pro the Purchased column.
6. **`.gitignore` re-ignores `.env.example`** (`.gitignore:73-75`, uncommitted). Appended `.env*` after `!.env.example`; `git check-ignore` now reports `.gitignore:75:.env*`. Both patterns already exist at `:42/:49`; lines are CRLF. Fix: drop the hunk.
7. **DEPLOY.md runbook errors** (`DEPLOY.md:379` "300 AI credits" while code grants 500 and the sandbox product was updated to that text; `:430` cross-ref to 5.4 is Index check, refresh is 5.5 step 5, which still says only `$29/mo`; `:455/:65/:1391` retired 30/60/300 packs, release check buys a pack the route refuses; `:681` env matrix omits `STRIPE_PRICE_ID_ANNUAL`; `:1104/:1162` `/lnkdrp` examples vs the new fatal `/lnkdrp-prod` check; `:1442` "no CI" false once test.yml lands; `:1650` stream-failure text stale). Fix: one docs pass.
8. **Stuck-job copy** (`src/lib/admin/cronSchedule.ts:172`). `Claimed running for ${since(runningFor)} . The function...`, stray space, untested. Fix: remove the space; assert `detail` in `cronSchedule.test.ts`.
9. **Em-dash rule not enforced where it matters** (`src/lib/prompts/visitBrief-system.md:9-11` still asks the model for em dashes and the email renders them verbatim; `tests/lib/noEmDashes.test.ts:45` pre-check `src.includes("—")` skips `\u2014` escapes, so `src/lib/email/templates/viewerIntroduction.ts:32, 74` preheaders pass green). Fix: drop the raw pre-check; either change the prompt shape or document model output as an exception and restore the preview sample.
10. **Sweep regression in visit-brief headlines** (`src/lib/ai/visitBrief.ts:157` joins object-shaped interests with " · "; `src/lib/visits/visitBriefs.ts:974` splits on `(p.`, ` —`, ` - ` only). Topic extraction fails for object-shaped output, subjects fall back to "page N". Fix: add `\s+·\s*` to the split, add a test; revert the `:971` comment hunk.
11. **Preflight Low finding not closed** (`src/lib/preflight/env.ts:170-176`). `new URL` still throws on comma host lists; `mongoUriDatabase()` from the same commit handles it. Also `:182` probes `client.db()` while `:193` judges `MONGODB_DB_NAME || path`. Fix: `const path = mongoUriDatabase(uri)`; probe `client.db(MONGODB_DB_NAME || undefined)`.
12. **Preflight and `/pricing` disagree on "annual is on"** (`src/lib/preflight/env.ts:66` vs `src/app/pricing/page.tsx:88` reading `BillingConfig.proAnnualPriceLabel`). Green preflight, monthly-only page until the admin refresh runs; stale label with env unset shows the toggle and checkout 400s. Fix: cross-check env var against the stored label in preflight; add "run the price refresh on /a/tools/billing" to 3.1.
13. **No behavioural test for the annual webhook path** (`tests/lib/annualBilling.test.ts:83-113`). Helpers tested; nothing asserts `$set.interval === "year"`, `kind: "pro"`, month-0 grant; three cases grep route source, one matches a 130-char line verbatim (`:112`), and `:111` claims the webhook consumes `metadata.interval`, which it never reads. Fix: add a yearly case to `stripeWebhook.test.ts` using its fixture; loosen the source matches.
14. **`spend` payload lies about plan** (`src/app/api/billing/spend/route.ts:113, 206-213`). `pro = onDemandEligible` so annual Pro gets `isPro: false`, `cycleEnd: null`, `needsCard: true`. Fix: keep `isPro` from `isProSubscription`, add `onDemandEligible` and `interval` fields.
15. **`scripts/preflight-env.ts:26`** not converted to `exit()` although it is the exact fetch-then-exit shape the helper targets. Fix: `await exit(s.fail ? 1 : 0)`.
16. **`tests/lib/imageImportClosure.test.ts:97`** sibling test still uses raw `path.relative`, passes vacuously on Windows. Fix: hoist a `relPosix` helper.
17. **`COMMANDS.md:58-74`** omits `npm test` and describes the old gate; `docs/TESTS.md` says the gate is `npm test`. Fix: add the row, reword `:74` (belongs in Agent A's Phase 0 commit).
18. **`realtime/server.ts:340` and `src/lib/db/access.ts:151`**. 40573 still takes the 30 s grace; `explainMongoAuthzError` always blames the database name, wrong for a role missing `changeStream` (the recorded dev incident); `:123` tells realtime operators about `MONGODB_DB_NAME`, which realtime ignores.
19. **`src/app/api/admin/billing/pro-price/route.ts:84`** accepts a yearly metered price (no `usage_type` check) while preflight rejects it; GET still returns only `proPriceLabel`.
20. **`src/app/dashboard/SubscriptionCard.tsx:98`** shows the monthly price under "Pro, yearly" when no annual label is stored; `:342-360` upgrade buttons not gated on `canManageBilling` (route 403s non-admins); `:171` M32 (`PlanPanel` inside render) untouched.
21. **Stale comments and docs the sweep or feature touched and left wrong**: `subscriptionState.ts:10` ("300 included", Phase 6), `:78` (`creditPacksAllowed` comment restates the 9.3c-vs-10c inversion backwards), `credits/purchase/route.ts:50` ("$39"), `:93` ("packs are for Free workspaces", now false as a statement), `creditsAdmin.ts:300`, `CreditsPurchaseClient.tsx:10, 454-456`, `credits/page.tsx:2-3, 69-71`, `SubscriptionCard.tsx:8`, `UpgradeModal.tsx:7`, `docs/FEATURES.md:350`, `docs/SUBSCRIPTION.md:35, 66-69, 73, 135`, `src/lib/admin/ui.ts:224`, `workspaceHub.ts:325` and four other "em dash" docstrings, `BillingConfig.ts:14`, `docs/reviews/pricing-upsell-fix-plan:13` (status line) and `:22` (0.2 bullet no longer matches what shipped).
22. **Low**: `src/lib/models/Subscription.ts:92-103` hot-reload patch omits `interval` (dev only); `webhook/route.ts:683` `invoice.paid` upserts by orgId with no foreign-id guard (pre-existing side door to 0.3); "2 months free" hard-coded in three places with no check that annual is 10x monthly; UpgradeModal `yearly` state persists across opens (`:110`); radiogroups lack arrow-key handling; `visitBriefEmail.ts:305` digest preheader uses " · " at both levels; `SignedOutCta` drops the yearly choice through sign-in (`PricingCta.tsx:61`).

## 5. Plan tasks now done or changed

- **Phase 0**: 0.3, 0.4, 0.5 are committed inside `bb19104`; 0.1, 0.2, 0.6, 0.7 remain uncommitted. 0.7's Accept ("CI is green") is currently unmet because of the `INDEX.md` failure. 0.2 gained the annual-forfeiture question (section 3.1). The 0.2 bullet text no longer matches what shipped (schedules cancel instead of refusing).
- **3.1**: partially done by B. Code side complete (interval field, checkout, webhook, toggle, modal, plan card, status payload, preflight, env/docs, helper tests). Remains: create the live price, set `STRIPE_PRICE_ID_ANNUAL` on Vercel, run the admin price refresh (add this step to the plan; the toggle keys off the stored label), the FAQ copy (issue 3), a route-level test, and the manual test-mode checkout.
- **3.3**: partially done by B, by a different resolution. Packs are open to annual Pro only; monthly Pro still gets 409 `PACKS_FREE_ONLY`, so the 9.3c-vs-10c inversion stands. Option A is now `creditPacksAllowed` returning true plus `CreditsPurchaseClient.tsx:207-210` `showPacks`, the 409 message, the two comments, and flipping `annualBilling.test.ts:62-63`. Option B is unaffected. Needs a product decision; the plan should record the annual-only interim.
- **3.2**: unchanged, harder. Stripe allows one interval per subscription, so the "annual twin" seat price is mandatory and `POST /api/stripe/seats` must pick by `Subscription.interval`.
- **1.1, 1.2**: unchanged, scope grew. Out-of-credits modal, banner, sidebar and `CreditsSummaryCard` need a third branch: annual Pro routes to `/credits`, not `/dashboard/limits`; `/api/billing/status.interval` is available for it.
- **4.1**: unchanged, easier. `interval` is in scope at `checkout/route.ts:134` and in session/subscription metadata.
- **Phase 6**: partially touched by B (`BillingConfig` now carries three labels via the admin refresh, not the hourly cache the plan asks for). New drift items to add: `DEPLOY.md:379, 410, 430, 455, 681, 1104, 1442, 1650`; `docs/SUBSCRIPTION.md:35, 66-69, 73, 135`; `subscriptionState.ts:78`; `purchase/route.ts:93`; the stale-comment list in issue 21. `subscriptionState.ts:10` "300" is still open.
- **M35** (not in the plan): partially addressed by B.
- **Low: preflight multi-host URI**: not fixed despite the file being edited.
- All other tasks (1.3 to 1.7, 2.1 to 2.3, 3.4, 3.5, 4.2, Phase 5) are unchanged; B touched files named by 1.3, 1.6 and 2.2 for punctuation only, and M23, M24, M32, H8 in those files are untouched.

## 6. Recommended next step

- **Unblock the tree first**: `git add` the untracked Phase 0 files, `npm run index`, run `npm test`, then commit the remaining Phase 0 set (0.1, 0.2, 0.6, 0.7, docs/TESTS.md, INDEX.md) so HEAD stops pointing at untracked files; update the fix-plan status line to say 0.3 to 0.5 are in `bb19104`. Revert or separately commit B's four leftovers (`.gitignore` drop entirely, `visitBriefs.ts` revert, `BillingInvoicesTab.tsx:1177` and `PlainChat.tsx` as a `copy:`/`ui:` commit).
- **Close the two money-path gaps before any annual price goes live**: `serviceCore.ts` `defaultIsProWorkspace` reading `interval` (issue 2, with the webhook clearing on-demand and the stale item id), and the annual forfeiture policy in `purge.ts` / org delete (section 3.1). Both are small and should land as a Phase 0.8 alongside a yearly case in `stripeWebhook.test.ts`.
- **Do 3.1's copy bullet now**, not in week 2: `pricing/page.tsx:258, 392, 396` plus `credits/page.tsx:69` and the `CreditsPurchaseClient.tsx:416` caption, since the page already sells the plan the copy contradicts. Add "run the admin price refresh" and "hide the toggle for existing Pro" to 3.1.
- **Run Phase 1 as planned with one adjustment**: 1.1 and 1.2 must include the annual-Pro branch (packs at `/credits`), and 1.2 should absorb the `CreditsSummaryCard` dead end. 1.3 to 1.7 are unaffected. Skip nothing in Phase 1; note every new string must avoid U+2014 or CI goes red.
- **Take the 3.3 decision explicitly** before Phase 3 work: B's annual-only compromise is coherent but leaves the inversion for monthly Pro; if option A, it is now a helper flip plus one test and a handful of comments. Record the interim in the plan either way.
- **Smaller B fixes to batch**: `cronSchedule.ts:172` space, `preflight/env.ts:172` `mongoUriDatabase`, `preflight-env.ts:26` `exit()`, `imageImportClosure.test.ts:97`, `noEmDashes.test.ts:45` pre-check, the visit-brief split/join pair, the DEPLOY.md pass (issue 7), and `COMMANDS.md` `npm test` row. Also worth relaying to Agent A: the 0.2 integration verifier found shared workspaces with another admin keep billing the leaver's Stripe customer (`account/delete/route.ts:99`), which the plan's 0.2 asked to cover.